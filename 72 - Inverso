#include <stdio.h>
#include <stdlib.h>
#define TAM 100

int main()
{
    int n, x, v[TAM];
    scanf("%d", &n);
    for(x=0;x<n;x++)
    {
        scanf("%d", &v[x]);
    }
    for(x=n-1; x>=0; x--)
    {
        if(x!=n-1)
            printf(" ");
        printf("%d", v[x]);
    }
    return 0;
}
