#include <stdio.h>
#include <stdlib.h>
#define TAM 101

int main()
{
    int n, x, y, v[TAM];
    for(n=0;n<TAM;n++)
    {
        scanf("%d", &v[n]);
    }
    x=v[100];
    for(y=0;y<n-1;y++)
    {
        if(x==v[y])
            printf("%d\n", y);
    }
    return 0;
}
