#include <stdio.h>

int mult(int x, int y, int z);

int main()
{
    int M, m, n;
    scanf("%d%d%d", &M, &m, &n);
    mult(M, m, n);
    return 0;
}

int mult(int x, int y, int z)
{
    int i, j;
    for (i=x; i<=y; i++)
    {
        if (i % z == 0)
            printf("%d\n", i);
    }
    return 0;
}
