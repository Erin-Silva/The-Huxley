#include <stdio.h>
#include <stdlib.h>


int main()
{
    int x, y, soma, i=0;
    scanf("%d%d", &x, &y);

   while (i!=x)
    {
        soma+=y;
        i+=1;
    }

    printf("%d", soma);
    return 0;
}
