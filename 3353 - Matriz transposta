#include <stdio.h>

int main()
{
    int m[101][101], l, c, i, j;
    scanf("%d%d", &l, &c);
    for (i=0; i<l; i++)
    {
        for (j=0; j<c; j++)
            scanf("%d", &m[i][j]);
    }
    for (i=0; i<c; i++)
    {
        for (j=0; j<l; j++)
            printf ("%d ", m[j][i]);

        printf ("\n");
    }
    return 0;
}
