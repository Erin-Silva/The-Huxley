#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char nome[51], sobrenome[51], full[102];
    scanf("%50s%*c", nome);
    scanf("%50s", sobrenome);
    strcpy(full, nome);
    strcat(full, " ");
    strcat(full, sobrenome);
    printf("%s\n", full);
    return 0;
}
